{% extends "base.html.twig" %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-offset-4 col-md-4">
                <div class="well">
                    <h1 style="text-align: center; margin: 2pc 0">Search City</h1>
                    {{ form_start(form) }}
                    {{ form_row(form.name) }}
                    <div style="text-align: center">{{ form_row(form.Search) }}</div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}